"""RabbitGhost — sovereign stealth console + Google-like homepage + semantic search.

An application layer built on the Rabbit mind (the ``rabbit`` package). Three faces:

* ``console``         — ghost stealth console (recon / cloak·stego / forge / network /
                        encrypt / decrypt / browse), all on Rabbit's sovereign organs.
* ``homepage``        — a sovereign, Google-like search homepage (stdlib http.server,
                        Gojo-gated/throttled/audited, binds 0.0.0.0, shows LAN/WireGuard/
                        egress IPs — no Tailscale).
* ``semantic_search`` — re-ranks web results by meaning, context, sentiment, and the
                        reasoning Dominance Engine's read of query intent.

The ``rabbit`` package (the mind) is a runtime dependency — see README.
"""

__version__ = "0.1.0"
