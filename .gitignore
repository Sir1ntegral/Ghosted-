# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Build artifacts
build/
dist/
*.spec
work/
*.log
installer_out/
.build-tmp/
# PyInstaller onedir bundles produced by build.ps1 / rebuild (exe output, not source)
Ghosted-FULL/
Ghosted-LEAN/
Ghosted-Full/
Ghosted-Lean/

# Local / runtime
.pytmp/
tmp/
*.db
logs/
.env
.env.*

# Local setup + private user data (history/favorites/credentials never leave the device)
.rabbitghost-setup
*.history.json
*.favorites.json
*.vault

# Rabbit runtime artifacts (written at repo-root cwd by the mind during tests/runs).
# Root-anchored so the packaged src/rabbitghost/data/ (trained model) is NOT ignored.
/quarantine/
/state/
/data/
/runtime/
/evidence/
/genesis/

# OS
Thumbs.db
.DS_Store
signing/azure-metadata.json
